<variant-01
 banners="{{banners}}"
            campaigns="{{campaigns}}"
            status="{{status}}"
            stores="{{stores}}"
            headerType="{{headerType}}"
             />
  <stores-list status="{{status}}"
              stores="{{stores}}"
              onSelect="onStoreSelect" />
<sticky-bottom />